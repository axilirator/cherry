{
	// Порт входящих соединений основного сервера.
	// Рекомендуемый диапазон 1024-49151.
	// По умолчанию: 19935.
	"main_port"     : 19935,

	// Порт входящих соединений файлового сервера.
	// Рекомендуемый диапазон 1024-49151.
	// По умолчанию: 19936.
	"fs_port"       : 19936,

	// Пароль, используемый для подключения.
	// По умолчанию: свободная аутентификация.
	"secret"        : false,

	// Максимально допустимое количество рабочих узлов.
	// По умолчанию: false (не ограничено).
	"max_clients"   : false,
	
	// Разрешает или запрещает подключение асинхронных узлов.
	// По умолчанию: true (разрешено).
	"async_allowed" : true,

	// Словарь для синхронизированного подбора паролей.
	"dictionary"    : false,

	// Атакуемый файл handshake.
	"capturefile"   : false
}